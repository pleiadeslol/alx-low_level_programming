#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
 * _islower - checks for lowercase character
 *
 * Return: 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	int islower(int c);
	
	if (islower(c) == 0)
	{
		return (0);
	} 
	else
	{
		return (1);
	}
}
