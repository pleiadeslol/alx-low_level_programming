#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * _islower - main function
 *
 * @c: character to compare
 *
 * Return: 1 if true, 0 if false
 *
 */

int _islower(int c)
{
	int islower(int c);

	if (islower(c) == 0)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
