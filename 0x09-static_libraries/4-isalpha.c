#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * _isalpha - main function
 *
 * @c: character to compare
 *
 * Return: 1 if true, 0 if false
 */

int _isalpha(int c)
{
	int isalpha(int c);

	if (isalpha(c) == 0)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
